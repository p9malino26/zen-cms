#!/usr/bin/env node

const path = require("path");

let compiled = path.resolve(__dirname, "../compiled/source-node/cli/index.js");
require(compiled);

let app = qx.core.Init.getApplication();
app.runCli();
