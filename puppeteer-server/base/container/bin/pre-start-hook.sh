# Add anything here that needs to be run (eg configuring environment variables)
# immediately before the node process is started.  It is not possible to kill
# the start.sh script because it is PID #1, so this is an opportunity to 
# influence the container at runtime (eg for debugging)
